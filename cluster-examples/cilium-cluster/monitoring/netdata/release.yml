apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: netdata
  namespace: monitoring
spec:
  interval: 1m
  timeout: 5m0s
  chart:
    spec:
      chart: netdata
      version: '3.x.x'
      sourceRef:
        kind: HelmRepository
        name: netdata-repo
        namespace: monitoring
      interval: 1m
